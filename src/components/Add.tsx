import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const Add = (): JSX.Element => {
  return (
    <SafeAreaView>
      <Text>Add</Text>
    </SafeAreaView>
  );
};

export default Add;
